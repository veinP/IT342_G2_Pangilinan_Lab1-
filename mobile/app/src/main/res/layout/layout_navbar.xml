<?xml version="1.0" encoding="utf-8"?>
<!-- Navbar: matches web .navbar (bg #2A4D87) -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:background="@color/hg_navy"
    android:elevation="4dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="16dp">

    <!-- Brand name: white like web .navbar-brand -->
    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:fontFamily="sans-serif-medium"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Welcome text: matches web .welcome-text (#D9D9D7) -->
    <TextView
        android:id="@+id/nav_welcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:text="Welcome!"
        android:textColor="@color/hg_card"
        android:textSize="13sp" />

    <!-- Logout button: matches web .btn-logout (bg #7C94B8, white text) -->
    <TextView
        android:id="@+id/nav_logout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_button_logout"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:text="@string/logout"
        android:textColor="@color/white"
        android:textSize="13sp"
        android:textStyle="bold" />
</LinearLayout>
